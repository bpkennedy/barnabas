---
label: ContentPage
hide_body: false
fields:
- name: publish
  type: datetime
  label: publish
  description: The date for the content to be published and visible to all website
    users.
  config:
    required: false
    date_format: 
    time_format: 
    display_utc: false
  default: now
- name: created
  type: datetime
  label: created
  description: The date the content was created (not necessarily the same as date
    published).
  config:
    required: true
    date_format: 
    time_format: 
    display_utc: false
  hidden: true
  default: now
- name: author
  type: select
  default: Mary Haggerty
  config:
    required: true
    options:
    - Mary Haggerty
    - Brian Kennedy
    source:
      type: simple
      section: 
      file: 
      path: 
  label: author
  description: The author of the content.
- name: title
  type: text
  config:
    required: true
  label: title
  description: The title of the content
- name: image
  type: file
  config:
    maxSize: 10
  label: image
  description: Upload a page image
